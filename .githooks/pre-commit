#!/bin/sh
# Build and validate the staged tree. The transaction owns Git's index.lock;
# failures leave staging untouched and preserve concurrent generated edits.
root="$(git rev-parse --show-toplevel)" || exit 1
exec python3 "$root/10_Agents/tools/skill_adapters/hook_transaction.py" "$root"
